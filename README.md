## Practice app for learning about React Server Components

`npx prisma generate`

`npx prisma studio` to see contents of db

`npm run faker-seed` if you need to seed the db

`npm run dev` to run the app locally
